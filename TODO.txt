Add value links: by setting the values of VAR to a different class. | done
Public variables | done
Make new exception classes for internal and user exceptions | done
Add More general tests | done
Hashmap for var name | done
Add Lists | done
Add multithreading with promise values. | done



Revise errors to make them more descriptive => Endgoal: Be able to write non-complexc code from some languages, and help the user to translate it to TASI

for some reson bool operations take rly long. Make it better

Add more methods.

Add custom operators

Add interpreter extentions

Add argument API

Limit memory usage by reusing values more often.

Add ways to obscure code and compile it into an intermediate language

Add Supervisor-Type namespaces

Add a proper debugger

After objects are done: Add precompiling to make object offsets static and known before the code is executed => Rquiers to implemment a pre execution type tracking system.


BETTER SYNTAX => A step foreward